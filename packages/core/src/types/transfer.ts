export enum DepositAction {
  DEPOSIT = "Deposit",
  WITHDRAW = "Withdraw",
}

export enum TransferTab {
  DEPOSIT = "Deposit",
  WITHDRAW = "Withdraw",
  ALLOCATE = "Allocate",
  DEALLOCATE = "Deallocate",
}
